{
  "id": "b7f1a2c0-1e1a-4a3a-9b0a-000000000002",
  "name": "spring-jpa-native (docker-compose)",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8080",
      "type": "default",
      "enabled": true,
      "description": "Hits the api service published by docker-compose.yml (api ports: 8080:8080) — same external port as local, just backed by the full docker-compose stack (postgres/redis/kafka/prometheus/grafana) instead of a locally-run JVM."
    },
    {
      "key": "token",
      "value": "",
      "type": "secret",
      "enabled": true
    }
  ],
  "_postman_variable_scope": "environment"
}
