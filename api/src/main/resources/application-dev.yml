spring:
  graphql:
    graphiql:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/productdb
    username: postgres
    password: postgres
  flyway:
    locations: classpath:db/migration,classpath:db/testdata
  data:
    redis:
      host: localhost
      port: 6379
management:
  health:
    redis:
      enabled: false
